- content_for(:title) { "Lenzie GC Conditions" }

%h1 New report

= simple_form_for [@source, @report] do |f| 
  = f.input :status, collection: ["Open", "Closed"]

  = f.input :weather, collection: ["Good", "Fair", "Poor"]

  = f.submit
